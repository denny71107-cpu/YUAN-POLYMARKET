const sleep=ms=>new Promise(r=>setTimeout(r,ms));
function findTab(){return [...document.querySelectorAll('button,[role="tab"],a')].find(e=>/代幣轉賬|Token transfers|Token Transfer/i.test(e.innerText||''))}
function bodyLines(){return (document.body.innerText||'').split(/\n+/).map(x=>x.trim()).filter(Boolean)}
function findEvidence(){const names=/BitoPro|Binance|Coinbase|Kraken|OKX|Bybit|Gate\.io|MEXC|KuCoin|Bitget|Bitopro/i;const els=[...document.querySelectorAll('body *')].filter(e=>e.children.length===0&&names.test(e.innerText||''));for(const el of els){let node=el;for(let i=0;i<6&&node;i++,node=node.parentElement){const text=(node.innerText||'').trim();if(text&&text.length<=3000){const txs=text.match(/0x[a-fA-F0-9]{64}/g)||[];if(txs.length)return {matchedText:(el.innerText||'').trim(),matchedBlock:text,matchedTxId:txs[0]}}} }return {matchedText:'',matchedBlock:'',matchedTxId:''}}
function collect(){const lines=bodyLines();const tags=lines.filter(x=/BitoPro|Binance|Coinbase|Kraken|OKX|Bybit|Gate\.io|MEXC|KuCoin|Bitget|Bitopro/i.test(x));const txId=(document.body.innerText.match(/0x[a-fA-F0-9]{64}/)||[''])[0];const address=(location.pathname.match(/0x[a-fA-F0-9]{40}/i)||[''])[0].toLowerCase();const evidence=findEvidence();return {address,matchedAddress:address,text:tags.join('\n'),txId,...evidence}}
let scanned=false;
async function scan(){if(scanned)return;scanned=true;let tab=findTab();if(tab&&!/token-transfer/i.test(location.pathname)){tab.click();await sleep(2500)}await sleep(1500);const p=collect();chrome.runtime.sendMessage({type:'OKLINK_RESULT',payload:p})}
chrome.runtime.onMessage.addListener(m=>{if(m.type==='SCAN')scan()});
setTimeout(()=>{chrome.runtime.sendMessage({type:'CONTENT_READY',tabId:chrome.runtime.id}).catch(()=>{})},1000);